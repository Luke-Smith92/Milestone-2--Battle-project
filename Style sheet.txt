body {
  font-family: Arial, sans-serif;
  text-align: center;
  background-color: #f0f8ff;
  padding: 20px;
}

.hidden {
  display: none;
}

.card-container {
  display: flex;
  justify-content: center;
  gap: 20px;
  margin-top: 20px;
}

.card {
  border: 2px solid #333;
  padding: 10px;
  width: 150px;
  background-color: white;
  cursor: pointer;
}

.card:hover {
  background-color: #f5f5f5;
}

/* HP bar styles */
.hp-bar {
  width: 120px;
  height: 12px;
  background: #ddd;
  border-radius: 6px;
  overflow: hidden;
  margin: 5px auto 0;
}

.hp-fill {
  height: 100%;
  background: green;
  width: 100%;
  transition: width 0.3s linear;
}
.battle-zone {
  display: flex;
  justify-content: center; /* horizontally center */
  align-items: center;     /* vertically center if taller container */
  gap: 50px;               /* space between player and AI cards */
  margin-top: 20px;
}
#store {
  margin-top: 40px;
}
.store-button {
  position: absolute;
  top: 20px;
  right: 20px;
  font-size: 1.2em; /* medium-large text */
  padding: 10px 18px;
  background-color: #ffcb05;
  color: #2a75bb;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  font-weight: bold;
}

.store-button:hover {
  background-color: #f5b800;
}
