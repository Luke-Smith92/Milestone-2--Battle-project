/* existing styles… */

.battle-zone {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 50px;
  margin-top: 20px;
}

/* make top-right button relative to the battle area */
#battleResult {
  position: relative;
}

/* Store button in the top-right of the battle section */
.store-button {
  position: absolute;
  top: 20px;
  right: 20px;
  font-size: 1.2em;
  padding: 10px 18px;
  background-color: #ffcb05;
  color: #2a75bb;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  font-weight: bold;
}
.store-button:hover { background-color: #f5b800; }

/* Optional back button style */
.back-button {
  margin-bottom: 15px;
  padding: 8px 14px;
  font-size: 1em;
  cursor: pointer;
}
